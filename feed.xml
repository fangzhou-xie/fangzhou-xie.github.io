<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.2">Jekyll</generator><link href="https://fangzhou-xie.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://fangzhou-xie.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2023-12-21T20:59:18+00:00</updated><id>https://fangzhou-xie.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal website of Fangzhou Xie. </subtitle><entry><title type="html">Read a Large CSV/TSV File and Write into Parquet/Feather by Arrow</title><link href="https://fangzhou-xie.github.io/blog/2023/arrow/" rel="alternate" type="text/html" title="Read a Large CSV/TSV File and Write into Parquet/Feather by Arrow"/><published>2023-03-20T00:00:00+00:00</published><updated>2023-03-20T00:00:00+00:00</updated><id>https://fangzhou-xie.github.io/blog/2023/arrow</id><content type="html" xml:base="https://fangzhou-xie.github.io/blog/2023/arrow/"><![CDATA[<h1 id="1-setup-rmd-for-blogs">1. Setup Rmd for blogs</h1> <p>In my last post, I mentioned that I used a newer template for my personal website. One thing I didn’t do was to bring my old posts into my new website. One of the posts was actually about me building a package to compile R Markdown file in Atom editor to plain HTML file for blog posts. As it turns out, this is no longer needed and there is an easier way to do this.</p> <p>I stole/adapted the YAML header from <a href="http://www.eroubenoff.net/2021-03-10-rmarkdown_jekyll/">Ethan Roubenoff</a>’s blog, which was “stole/adapted” (original wording of Ethan) from <a href="http://svmiller.com/blog/2019/08/two-helpful-rmarkdown-jekyll-tips/">Steven Miller</a>’s post. This will allow us to write in Rmd and then compile into the corresponding post folder for display on website.</p> <p>Some tweaks were needed in my case including adding the <code class="language-plaintext highlighter-rouge">category</code> and <code class="language-plaintext highlighter-rouge">tags</code> fields and the output folder. Here is the YAML for this blog post.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">title</span><span class="pi">:</span> <span class="s">Transform a Large CSV/TSV File into Arrow</span>
<span class="na">author</span><span class="pi">:</span> <span class="s">Fangzhou Xie</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2023-03-20</span>
<span class="na">output</span><span class="pi">:</span>
  <span class="na">md_document</span><span class="pi">:</span>
    <span class="na">variant</span><span class="pi">:</span> <span class="s">gfm</span>
    <span class="na">preserve_yaml</span><span class="pi">:</span> <span class="s">TRUE</span>
<span class="na">knit</span><span class="pi">:</span> <span class="s">(function(inputFile, encoding) {</span>
  <span class="s">rmarkdown::render(inputFile,</span> 
                    <span class="s">encoding = encoding,</span> 
                    <span class="s">output_file=paste0(Sys.Date(), "-", sub(".Rmd", ".md",inputFile)),</span> 
                    <span class="s">output_dir = "../_posts") })</span>
<span class="na">layout</span><span class="pi">:</span> <span class="s">post</span>
<span class="na">category</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">programming</span><span class="pi">,</span> <span class="nv">website</span><span class="pi">]</span>
<span class="na">tags</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">arrow</span><span class="pi">,</span> <span class="nv">Rmd</span><span class="pi">,</span> <span class="nv">R</span><span class="pi">]</span>
<span class="na">always_allow_html</span><span class="pi">:</span> <span class="kc">true</span>
<span class="na">related_posts</span><span class="pi">:</span> <span class="kc">false</span>
<span class="nn">---</span>
</code></pre></div></div> <h1 id="2-my-failed-attempt-to-follow-the-arrow-tutorials">2. My failed attempt to follow the Arrow tutorials</h1> <p>But what I really wanted to say was my recent discovery of R package <a href="https://cran.r-project.org/web/packages/arrow/index.html"><code class="language-plaintext highlighter-rouge">arrow</code></a>. This will allow us to work with very large dataset (those even larger than RAM) efficiently. A wonderful introduction of why <code class="language-plaintext highlighter-rouge">parquet</code> format is more suitable in those situations can be found in <a href="https://r4ds.hadley.nz/arrow.html#sec-parquet">R for Data Science</a>.</p> <p>However, if we follow the steps in the <a href="https://r4ds.hadley.nz/arrow.html#rewriting-the-seattle-library-data">Section 23.4.3</a>, we will encounter the following code block:</p> <div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">seattle_csv</span><span class="w"> </span><span class="o">|&gt;</span><span class="w">
  </span><span class="n">group_by</span><span class="p">(</span><span class="n">CheckoutYear</span><span class="p">)</span><span class="w"> </span><span class="o">|&gt;</span><span class="w">
  </span><span class="n">write_dataset</span><span class="p">(</span><span class="n">path</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">pq_path</span><span class="p">,</span><span class="w"> </span><span class="n">format</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"parquet"</span><span class="p">)</span><span class="w">
</span></code></pre></div></div> <p>where the <code class="language-plaintext highlighter-rouge">seattle_csv</code> object was defined earlier in <a href="https://r4ds.hadley.nz/arrow.html#opening-a-dataset">Section 23.3</a> as:</p> <div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">seattle_csv</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">open_dataset</span><span class="p">(</span><span class="w">
  </span><span class="n">sources</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"data/seattle-library-checkouts.csv"</span><span class="p">,</span><span class="w"> 
  </span><span class="n">format</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"csv"</span><span class="w">
</span><span class="p">)</span><span class="w">
</span></code></pre></div></div> <p>If we combine those two blocks we have the following:</p> <div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">seattle_csv</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">open_dataset</span><span class="p">(</span><span class="w">
  </span><span class="n">sources</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"data/seattle-library-checkouts.csv"</span><span class="p">,</span><span class="w"> 
  </span><span class="n">format</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"csv"</span><span class="w">
</span><span class="p">)</span><span class="w">

</span><span class="n">seattle_csv</span><span class="w"> </span><span class="o">|&gt;</span><span class="w">
  </span><span class="n">group_by</span><span class="p">(</span><span class="n">CheckoutYear</span><span class="p">)</span><span class="w"> </span><span class="o">|&gt;</span><span class="w">
  </span><span class="n">write_dataset</span><span class="p">(</span><span class="n">path</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">pq_path</span><span class="p">,</span><span class="w"> </span><span class="n">format</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"parquet"</span><span class="p">)</span><span class="w">
</span></code></pre></div></div> <p>This seems pretty straightforward, right? The first block read the data in CSV and the second block write the data into <code class="language-plaintext highlighter-rouge">parquet</code> format.</p> <p>I felt very confident, and tried to used those two blocks of codes to read and write an extremely large TSV file I have (&gt;40 GB) into <code class="language-plaintext highlighter-rouge">parquet</code> format. However, I tried it multiple times and it always ended up occupying all of my RAM (64 GB) and froze my desktop.</p> <div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">my_big_tsv</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">open_dataset</span><span class="p">(</span><span class="n">sources</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"big.tsv"</span><span class="p">,</span><span class="w"> </span><span class="n">format</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"tsv"</span><span class="p">)</span><span class="w">

</span><span class="n">my_big_tsv</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w"> 
  </span><span class="n">group_by</span><span class="p">(</span><span class="n">year</span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w"> 
  </span><span class="n">write_dataset</span><span class="p">(</span><span class="n">path</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"big-parquet"</span><span class="p">)</span><span class="w">
</span></code></pre></div></div> <p>I was exactly following the recipe but why didn’t it work?</p> <h1 id="3-my-solution-to-deal-with-large-csvtsv-files">3. My solution to deal with large CSV/TSV files</h1> <p>I then did some online surfing to figure out why this is the case. The official <a href="https://arrow.apache.org/cookbook/r/reading-and-writing-data---multiple-files.html#discussion-5">Apache Arrow documentation for R</a> clears states: “There are two different ways to specify variables to use for partitioning - either via the <code class="language-plaintext highlighter-rouge">partitioning</code> variable as above, or by using <code class="language-plaintext highlighter-rouge">dplyr::group_by()</code> on your data - the group variables will form the partitions.”</p> <p>This wording seems to suggest that both ways are equivalent to each other. Either we group before writing or we specify the group in the argument when writing the dataset. But I am stuck with the <code class="language-plaintext highlighter-rouge">dplyr::group_by()</code> route and why not try the other way?</p> <div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">my_big_tsv</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">open_dataset</span><span class="p">(</span><span class="n">sources</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"big.tsv"</span><span class="p">,</span><span class="w"> </span><span class="n">format</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"tsv"</span><span class="p">)</span><span class="w">

</span><span class="n">my_big_tsv</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w"> 
  </span><span class="n">write_dataset</span><span class="p">(</span><span class="n">path</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"big-parquet"</span><span class="p">,</span><span class="w"> </span><span class="n">partitioning</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"year"</span><span class="p">))</span><span class="w">
</span></code></pre></div></div> <p>And this time the machine only took a litter CPU and RAM to produce parquet files.</p> <p>It seems reasonable now. The <code class="language-plaintext highlighter-rouge">open_dataset</code> will not read the actual data into memory unless it was requested. The <code class="language-plaintext highlighter-rouge">group_by</code> actually requests the data to be loaded for the grouping, before writing into the <code class="language-plaintext highlighter-rouge">parquet</code> format.</p> <p>However, without the explicit <code class="language-plaintext highlighter-rouge">dplyr::group_by</code>, the grouping was carried out by arrow directly in the <code class="language-plaintext highlighter-rouge">partitioning</code> argument. Hence there is no need to read the entire dataset into memory for grouping.</p> <p>Therefore, the two different approaches are not equivalent to each other. The <code class="language-plaintext highlighter-rouge">group_by</code> method will suffocate the machine when dealing with larger-than-RAM files, whereas the <code class="language-plaintext highlighter-rouge">partitioning</code> method will not. In my case, the file was smaller than RAM, but the <code class="language-plaintext highlighter-rouge">group_by</code> operation clearly needs memory several times of the original file, so that the entire operation becomes a larger-than-RAM situation.</p> <p>The whole point of using Arrow was to avoid loading the whole dataset into memory, but if we really need to <code class="language-plaintext highlighter-rouge">group_by</code> before writing into the <code class="language-plaintext highlighter-rouge">parquet</code> and if this process actually suffocates the memory, why should we do it this way? I think this difference should be at least mentioned somewhere by someone, but I haven’t found anyone doing so. Hence I documented my struggle in the hope of helping someone in the future.</p> ]]></content><author><name></name></author><category term="[&quot;programming&quot;, &quot;website&quot;]"/><category term="arrow"/><category term="Rmd"/><category term="R"/><summary type="html"><![CDATA[1. Setup Rmd for blogs]]></summary></entry><entry><title type="html">A New Look of My Website</title><link href="https://fangzhou-xie.github.io/blog/2023/website-change/" rel="alternate" type="text/html" title="A New Look of My Website"/><published>2023-03-15T00:00:00+00:00</published><updated>2023-03-15T00:00:00+00:00</updated><id>https://fangzhou-xie.github.io/blog/2023/website-change</id><content type="html" xml:base="https://fangzhou-xie.github.io/blog/2023/website-change/"><![CDATA[<p>While trying to update my website, I found that my previous website template <a href="https://github.com/academicpages/academicpages.github.io">academicpages.github.io</a> has not been updated for some time and was only working with Ruby 2.x. Since Ruby upgrades to 3.x, it was backward incompatible and caused some issues. I could potentially learn a new language and make it work, but I discovered this other academic website template <a href="https://github.com/alshedivat/al-folio">al-folio</a>. It seems to be more update-to-date and modern, so I decided to use this design for my website.</p> <p>Although I posted some blogs in the old website and even wrote a little package to use Atom for automatically compiling Rmd into Markdown for the website, I didn’t quite like the way it was displaying the blog posts. Plus, I later shifted to Visual Studio Code as my main editor (like everyone should).</p>]]></content><author><name></name></author><category term="website"/><category term="website"/><summary type="html"><![CDATA[Changing my personal website to a better-looking and modern template]]></summary></entry></feed>